{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the PDF watermark tool.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
